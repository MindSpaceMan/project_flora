<!doctype html>
<html lang="ru">
<head>
    <meta charset="UTF-8" />
    <title>Новый заказ №{{ order.id }}</title>
</head>
<body style="font-family: Arial, sans-serif; color:#222;">
<h2>Новый заказ №{{ order.id }}</h2>
<p>Статус: <strong>{{ order.status }}</strong></p>

<h3>Контакты клиента</h3>
<ul>
    <li>Имя: {{ fullName ?: '—' }}</li>
    <li>Email: {{ email ?: '—' }}</li>
    <li>Телефон: {{ phone ?: '—' }}</li>
</ul>

<h3>Доставка</h3>
<ul>
    <li>Адрес: {{ address ?: '—' }}</li>
    <li>Комментарий: {{ comment ?: '—' }}</li>
    <li>Согласие на рассылку: {{ newsletter ? 'Да' : 'Нет' }}</li>
</ul>

{% if order.items is defined and order.items|length > 0 %}
    <h3>Состав заказа</h3>
    <table cellpadding="6" cellspacing="0" border="1" style="border-collapse: collapse;">
        <thead>
        <tr>
            <th align="left">#</th>
            <th align="left">Товар</th>
            <th align="left">Кол-во</th>
        </tr>
        </thead>
        <tbody>
        {% for item in order.items %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ item.productTitle ?? ('Товар #' ~ loop.index) }}</td>
                <td>{{ item.quantity }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endif %}

<p style="margin-top:16px;">Пожалуйста, обработайте заказ.</p>
</body>
</html>